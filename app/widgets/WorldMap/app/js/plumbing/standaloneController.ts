/// <reference path="../../../../../includes/typescript/angular.d.ts" />
angular.module('wsAngular').controller('standaloneCtrl', ['$scope',
    function ($scope) {
        $scope.mapData = {};

        //$scope.tableData = new TableData($scope.permissionInfo);
        //$scope.applyChanges = function(param1,param2) {
        //    alert($scope.nodeInfo.nodeID);
        //    alert("Applying changes using param "+param1 + "param2" + param2);
        //}
    }
]);

